{
  "mcpServers": {
    "mcp-gateway": {
      "type": "http", 
      "url": "http://localhost:${MCP_GATEWAY_PORT}",
      "headers": {
        "Authorization": "Bearer ${MCP_GATEWAY_AUTH_TOKEN}"
      }
    }
  }
}
